import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';


# Nav (Native)

## Tag: &lt;nav&gt;&lt;/nav&gt;

### MENU PEGAJOSO (STICKY MENU) [NATIVE]

export const NavbarOne = () => {

	const {NavNative} = require('@site/src/widgets/w-tags/Nav-tag');

	return(
		<>
			<NavNative/>
		</>
	)
};

<NavbarOne/>

<Tabs>
  <TabItem value="html" label="HTML" default>

```html title="Sticky Menú Code"
<nav id="navbar">
  <label for="chkToggle">
    <i>≡</i>
  </label>
  <a href="#">logo</a>
  <input type="checkbox" id="chkToggle"></input>
  <ul>
    <li>
      <a href="#">Home</a>
    </li>
    <li>
      <a href="#">Products</a>
    </li>
    <li>
      <a href="#">About Us</a>
    </li>
    <li>
      <a href="#">Contact Us</a>
    </li>
    <li>
      <a href="#">Blog</a>
    </li>
  </ul>
</nav>
```

  </TabItem>
  <TabItem value="css" label="CSS">
```css title="Sticky Menu style"
nav#navbar {
  background: rgba(0,0,0,0.9);
  position: sticky; /*A diferencia del fixed, sticky si ocupa espacio, debe estar acompañado por top*/
  top:0;
  z-index: 10;
  padding-bottom: 10px;
}

nav#navbar > ul {
  list-style-type: none;
  display: none;
}

nav#navbar > a, nav#navbar > ul > li > a {
  text-decoration: none;
  color: rgba(255, 255, 255, 0.7);
}

nav#navbar > ul > li > a {
  text-align: center;
}

nav#navbar > a {
  display: inline-block;
  font-size: 22px;
  margin-top: 10px;
  margin-left: 20px;
}

nav#navbar > label {
  position: absolute;
  top: 10px;
  right: 20px;
  cursor: pointer;
  color: rgba(255, 255, 255, 0.8);
  font-size: 24px;
}

#chkToggle {
  display: none;
}

nav#navbar > ul {
  list-style-type: none;
  /* NUEVO: flex para centrar en móvil */
  display: none;
  flex-direction: column;
  align-items: center;
  gap: 15px 0px;
}

nav#navbar > input:checked + ul {
  display: flex;
}

@media (width >= 768px) {
  nav#navbar {
    display: flex;
    justify-content: space-between;
    padding-bottom: 0;
    height: 70px;
    align-items: center;
  }

  nav#navbar > input:checked + ul {
    display: flex;
  }

  nav#navbar > ul {
    display: flex;
    margin-right: 30px;
    flex-direction: row;
    justify-content: flex-end;
  }

  nav#navbar > ul > li {
    margin: 0;
  }

  nav#navbar > ul > li > a {
    margin-left: 40px;
  }

  nav#navbar > a {
    margin-top: 0;
  }

  nav#navbar > label {
    display: none;
  }

  nav#navbar > a:hover,
  nav#navbar > ul > li > a:hover {
    color: rgba(255, 255, 255, 1);
  }
}
```
  </TabItem>
</Tabs>
